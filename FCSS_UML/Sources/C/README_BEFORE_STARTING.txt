1) Only help functionality is working and it is working in all interfaces
2) make install will only work in a FreeBSD system, as i couldn't make it work in other OS's
3) Language options are working, so if necessary, it's possible to open portuguese Ncurses interface with ./fcssUml -N language=portuguese, for instance
4) The program reads present at fcss-uml.cfg file and loads the options written into it
5) All password functionalities are tested and working e.g: fcssUmlCheckPassword, FcssUmlEncodePasswordWithSpecificSalt ... 